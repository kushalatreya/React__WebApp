//currently this is not in use.

import React from 'react';

function BannerButton(){
    return(
         <button className="start-btn" autoFocus={true}>Get started</button>
    );
}

export default BannerButton;